// 6 - crie um algoritmo que converte dias em horas, 
//quando recebe um número de dias.

function converte(dias) {
    const diasHoras = (dias*24) 
    return `A quantidade de dias convertida é ${diasHoras} h`
}

const resultado = converte (2)
console.log(resultado)

